debug: true

server:
  port: 1000
  servlet:
    context-path: /api

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1000/api/eureka/
    register-with-eureka: false
    fetch-registry: false
