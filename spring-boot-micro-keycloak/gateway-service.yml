debug: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1000/eureka/

server:
  port: 1002
  servlet:
    context-path: /api

#actuator / management
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
